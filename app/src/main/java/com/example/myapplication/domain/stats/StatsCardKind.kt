package com.example.myapplication.domain.stats

/** Три карточки колоды статистики. Порядок enum = порядок карточек в колоде. */
enum class StatsCardKind {
    RATING_BY_GENRE,
    GENRE_FREQUENCY,
    OVERVIEW,
}
