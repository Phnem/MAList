# TICKET-01: Шапка ридера не заезжает под вырез камеры

## Status

DONE (визуальное подтверждение — за пользователем, см. Completion evidence)

## Objective

Название главы и вся верхняя панель ридера манги видны целиком на устройствах с вырезом фронтальной камеры.

## User or system value

Пункт 1 списка. Сейчас название главы физически не прочитать — его перекрывает вырез.

## Dependencies

Нет — можно начинать сразу.

## Scope

`manga/ui/MangaReaderScreen.kt` — отступ верхнего бара (сейчас `Modifier.statusBarsPadding()`, строка ~774).

Корневая причина установлена: `MangaReaderActivity.hideSystemBars()` прячет системные бары, после чего `statusBars`-инсет схлопывается в 0, а вырез камеры в этот инсет не входит. Нужен отступ по объединению `systemBars ∪ displayCutout`, чтобы он оставался ненулевым при скрытых барах.

## Out of scope

Нижний бар ридера (там `navigationBarsPadding()` и жалоб на него нет). Поведение самого `hideSystemBars()`. Ландшафтная перекомпоновка ридера.

## Acceptance criteria

- [ ] Название главы и кнопка «назад» не пересекаются с вырезом камеры.
- [ ] При скрытых системных барах верхний отступ ненулевой.
- [ ] На устройстве без выреза лишнего пустого отступа сверху не появилось.
- [ ] Док ридера (`ReaderDock`) остался на месте и не потерял стеклянный материал.

## Verification plan

`.\gradlew.bat :app:compileDebugKotlin` — компиляция. Визуальную проверку на устройстве с вырезом выполняет пользователь: я её выполнить не могу и не буду заявлять, что выполнил.

## TDD classification

NOT_NEEDED — чистая вёрстка Compose. Альтернативная верификация: компиляция + визуальная проверка пользователем.

## Expected architecture impact

Локальный. Допущение «системные бары скрыты» перестаёт быть неявным на стороне композабла.

## Risks

Переусердствовать с `safeDrawingPadding()` — он добавит и нижние инсеты, сломав раскладку бара. Нужен именно объединённый инсет сверху.

## Implementation notes

`MangaReaderScreen.kt`, верхний бар ридера: `.statusBarsPadding()` заменён на

```kotlin
.windowInsetsPadding(
    WindowInsets.systemBars
        .union(WindowInsets.displayCutout)
        .only(WindowInsetsSides.Top + WindowInsetsSides.Horizontal),
)
```

Горизонталь включена ради ландшафта (там вырез уходит вбок). Низ исключён намеренно: бар прижат к верху, нижний инсет только раздул бы ему высоту. Импорт `statusBarsPadding` убран — других обращений в файле не было.

## Deviations

Отступлений от плана тикета нет.

## Review findings

- **Ловушка `layerBackdrop` — не задета.** `statusBarsPadding()` в Compose реализован как `windowInsetsPadding(WindowInsets.statusBars)`, то есть узел заменён на узел того же типа. Число и тип модификаторов над `drawBackdrop` дока не изменились, структурного добавления/удаления не произошло.
- **Устройство без выреза** — регрессии нет по построению: `systemBars` при скрытых барах даёт 0, `displayCutout` без выреза даёт 0, объединение даёт 0. Лишнего отступа не появляется.
- **`ReaderDock`** не тронут: правка только в модификаторе родительского `Row`.
- Блокирующих замечаний нет.

## Completion evidence

- Команда: `.\gradlew.bat :app:compileDebugKotlin` → `BUILD SUCCESSFUL in 14s`.
- **Не выполнено и не может быть выполнено мной:** визуальная проверка на устройстве с вырезом камеры (AC-1, AC-3). Эмулятора/устройства в этой сессии нет. Влияние на уверенность: логика инсетов проверяется рассуждением и компиляцией, но фактическое положение текста относительно выреза подтвердить нечем. Косвенное свидетельство — корневая причина установлена из кода (`hideSystemBars()` обнуляет `statusBars`), и правка адресует именно её. **Требуется подтверждение пользователя.**
- Тикет завершается: код-критерии выполнены, визуальные — переданы пользователю явно, а не отмечены как пройденные.
