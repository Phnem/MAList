# TICKET-05: Правка формы движения по референсу пользователя

## Status

BLOCKED — ждёт референс от пользователя. **Реализацию не начинать.**

## Objective

Привести композицию и форму движения к тому, что пользователь имел в виду. Текущий вариант
принят как промежуточный.

## User or system value

Пользователь, 2026-07-29, после просмотра анимации: «это не то, что я имел в виду, но пока что —
норм, вшивай, я не смогу словами объяснить как я хотел, я сделаю анимацию как референс, покажу
тебе позже и ты подредактируешь, но пока что закрывай этот вариант».

Формулировать за него не нужно: он покажет референс.

## Dependencies

Заблокирован **референсом**, не кодом. Технически TICKET-02 уже даёт всё нужное.

## Scope

Пока не определён. Ожидаемо затрагивает:

- `Rig` в `ui/shared/loading/KatanaLoader.kt` — пропорции, базовый угол, радиус, расположение
  ножен относительно окружности;
- возможно `KatanaCycle` — если поменяются границы фаз или характер инерции.

Точки применения (ридер манги, плеер) правки не потребуют: они знают только
`KatanaLoadingOverlay`.

## Out of scope

Всё, пока не показан референс.

## Acceptance criteria

Не определены — заполнить после референса.

## Verification plan

Не определён. Как минимум: `:app:compileDebugKotlin` и повторный показ анимации пользователю.

## TDD classification

Зависит от объёма. Если поедут границы фаз — тесты `KatanaCycleTest` придётся править вместе с
ними, а не удалять.

## Expected architecture impact

Нулевой: правится только слой рисования, публичный `KatanaLoadingOverlay` не меняется. Это и
было целью разделения на три слоя.

## Risks

Соблазн угадать желаемое до референса. Не угадывать — текущий вариант рабочий и принят.

## Implementation notes

Задел, оставленный намеренно: `KatanaCycle.scabbardAngularLag` (угловое отставание ножен на
обороте) реализован и покрыт тестом, но не используется — ножны сейчас неподвижны во время
оборота. Если в референсе ножны волочатся за катаной по кругу, функция уже готова.

## Deviations

## Review findings

## Completion evidence
