# TICKET-04: Переделка загрузки — полноэкранные состояния

## Status

NEEDS_USER_DECISION — ожидает описания от пользователя. **Реализацию не начинать.**

## Objective

Не определён. Пользователь: «Нужно переделать загрузку: воспроизведение аниме, манги, прогрузка details и другие, где подойдёт полноэкранная загрузка, этот пункт запиши в план, я его опишу позже, отдельным проходом».

## User or system value

Пункт 4 списка. Ценность не сформулирована — известно только направление: полноэкранные состояния загрузки вместо нынешних, в нескольких местах приложения.

## Dependencies

Нет технических блокировок. Заблокирован **описанием требований**.

## Scope

Пока не определён. Названные пользователем области:

- воспроизведение аниме;
- воспроизведение манги;
- загрузка Details;
- «и другие, где подойдёт полноэкранная загрузка».

Что известно из дискавери прогона `vetro-player` и предыдущего `vetro-todo`: сейчас индикаторы загрузки точечные и разнородные — `CircularProgressIndicator(color = BrandOrange)` в `MangaReaderScreen:146,689`, свой индикатор в `MangaChaptersPage:122`, свои состояния в Details. Общего компонента полноэкранной загрузки нет. Это наблюдение, а не решение: каким должен быть новый вид, определяет пользователь.

## Out of scope

Всё, пока не описано.

## Acceptance criteria

Не определены — будут заполнены после описания.

## Verification plan

Не определён.

## TDD classification

Не определена.

## Expected architecture impact

Вероятно появление общего компонента полноэкранной загрузки и переход разрозненных индикаторов на него. **Предположение, а не план** — не использовать как основание для работы.

## Risks

Затрагивает много экранов сразу. Начинать без описания нельзя: угаданный вид загрузки придётся переделывать по всем местам разом.

## Implementation notes

## Deviations

## Review findings

## Completion evidence
