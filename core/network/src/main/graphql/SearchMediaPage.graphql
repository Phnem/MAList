query SearchMediaPage($q: String, $page: Int, $perPage: Int, $type: MediaType = ANIME) {
  Page(page: $page, perPage: $perPage) {
    media(search: $q, type: $type) {
      id
      title {
        romaji
        english
      }
      description
      type
      status
      episodes
      coverImage {
        large
      }
      averageScore
      genres
    }
  }
}
